<%= form_for(@pizza_place) do |f| %>
  <% if @pizza_place.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pizza_place.errors.count, "error") %> prohibited this pizza_place from being saved:</h2>

      <ul>
      <% @pizza_place.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
